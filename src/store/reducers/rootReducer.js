const initState = {
  users: [
    { id: 1, name: "BPR" },
    { id: 2, name: "dacbac2702" },
    { id: 3, name: "BPRxBACx6" },
  ],
};

const rootReducer = (state = initState, action) => {
  return state;
};

export default rootReducer;
